---
import BlogPostSection from "../components/BlogPostSection/BlogPostSection.astro"
import Footer from "../components/Footer/Footer.astro"
import Header from "../components/Header/Header.astro"
import Layout from "../layouts/Layout.astro"
import "../styles/components/SearchPage.css"
import "../styles/global.css"

const category = Astro.url.searchParams
---

<Layout>
	<Header />
	<div class="searchContainer">
		<div class="searchPageContainer">
			<div class="searchHeaderContainer">
				<div class="headerTitle" id="Title">#{category}</div>
			</div>
		</div>
	</div>
	<script>
		// Get the query parameters from the current URL
		const queryParams = new URLSearchParams(window.location.search)
		const BlogPostElement = document.getElementById("BlogPost") as HTMLElement

		const searchTer = queryParams.get("search")

		console.log("Message:", queryParams.get("search"))
	</script>
	<BlogPostSection name="lifeStyle" id="BlogPost" />
	<Footer />
</Layout>
